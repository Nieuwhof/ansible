---

- name: Create user account
  user: name=ops
        password={{ set_pass|password_hash('sha512') }}
        state=present
        append=yes
        shell="/bin/bash"
        update_password=always
       
- name: Print some debug information 
  vars: 
    msg: |
        Ansible Distribution: {{ password }}
        Ansible Dist version: {{ ansible_distribution_version }}
  debug: 
    msg: "{{ msg.split('\n') }}"  