---
- hosts: all
  gather_facts: false
  connection: local
  become: true
  become_user: root
  become_method: "su"
  become_exe: 'sudo -p "Password: " su -'

  roles:
    - createoceanvps
     
     
