---
- hosts: all
  become: true
  become_user: root
  
  vars:
   account:
    - name: arnold 
      group: sudo
    - name: bolti
      group: sudo
    
  tasks:
     - name: Loop test create users
       user:
          name: '{{ item }}' 
          group: '{{ item }}' 
          state: present
       loop: '{{ account }}'

